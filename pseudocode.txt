Start
Turn on the phone
Press button Freqfood.app
Press button "menu list food"
READ AND WRITE "Food Name" with string
IF WHILE "Food Name" Food not available "menu"
          PRINT "Menu not available"
          Do "READ AND WRITE "Food Name" with string"
ELSE
    READ AND WRITE "Amount of Food" with number
END IF
Press button "food confirmation"
IF WHILE want to add food menu "menu"
         Do "READ AND WRITE "Food Name" with string"
ELSE
    Press button "shopping cart menu"
END IF
Pess button "food checklist"
READ AND WRITE "Enter shipping address" with string
Press button "make payment"
READ AND WRITE "proof of payment" with string
Finish
    
